{
  "name": "@mcp-server/rag-engine",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test:unit": "vitest run --mode=test unit",
    "test:integration": "vitest run --mode=test integration",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mcp-server/shared-types": "workspace:*",
    "@mcp-server/arcgis-client": "workspace:*",
    "langchain": "^0.1.0",
    "@xenova/transformers": "^2.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
