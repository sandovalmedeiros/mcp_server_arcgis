{
  "name": "@mcp-server/arcgis-client",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test:unit": "vitest run --mode=test unit",
    "test:integration": "vitest run --mode=test integration",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mcp-server/shared-types": "workspace:*",
    "@arcgis/core": "^4.28.0",
    "undici": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
